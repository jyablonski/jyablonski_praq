[project]
name = "mcp-rag-server"
version = "0.1.0"
description = "Minimal MCP 2026-07-28 Streamable HTTP server and client"
readme = "README.md"
requires-python = "==3.12.*"
dependencies = [
    "mcp==2.0.0",
    "uvicorn>=0.38.0",
]
